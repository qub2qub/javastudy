1 зачем ввсели обёртки на примитивами
(чем отличаются, где и как используются)

2 что лучше для многопоточности: примитивы или обёртки (как и почему)

3 абстрактный класс - что это. может ли класс быть абс без абс метода? чем отличается от интерфейса?

4 что может быть в интерфейсе

5 где можно использовать final, что будет если класс final.

6 immutable объекты - что это значит. примеры, где используются. зачем sting сделаны immutable?

7 про коллекции
- основные интерфейсы, чем отличаются.
-отличия linked list от arraylist (тут меня климануло и я ваще запутался)
-map позволяет дубликаты ключей? (зависит от реализации)

8 как работает dependency injection в спринге? если есть autowired у Private поля, как спринг сеттит это поле?

9 какие есть bean scope в спринге, какой по умолчанию? что значит request scope если приложение НЕ для веба?
prititype - каждый раз создаёт новый экземпляр
singleton - 1 раз на всё время жизни приложения
request - на каждый реквест
session - на сессию.

10 понятие о рест приложении. 
он по максимуму использует возможности http протокола. без него - это не рест.
stateles - т.к. не хранит информацию о своём сотоянни, прошлых вызовах, частоте обращения к бд например, а "каждый раз как в первый раз".



11 